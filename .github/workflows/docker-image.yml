name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag giteeru/s3uploaddir
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ giteeru }}
        password: ${{ dckr_pat_NH36mts7HITYTw93J0fFiEzCpmw }}  
    - name: Push the Docker image
      run: docker push giteeru/s3uploaddir
